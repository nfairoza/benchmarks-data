$ sudo visudo

# Add the following line at the end of the file

root ALL=(ALL) NOPASSWD: ALL
bnetflix ALL=(ALL) NOPASSWD: ALL

# Verify it
sudo -k  # Forces password request on the next sudo
sudo whoami


